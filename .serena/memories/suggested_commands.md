# よく使うコマンド

## セットアップ
- `npm install`

## 開発
- `npm run dev`（http://localhost:3000 / MCP: http://localhost:3000/api/mcp）

## ビルド/起動
- `npm run build`
- `npm start`

## 追加（scripts）
- `bash scripts/validate-config.sh`（未確認: 何を検証するかは未読）
- `node scripts/simple-fetch.mjs`（未確認）
- `node scripts/test-hourly.mjs`（未確認）
